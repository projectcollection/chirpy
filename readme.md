# chirpy

my first server writtend in go


## dev

run in debug mode: resets the file db on start
```
go build -o out && ./out --debug
```

## endpoints

```
POST /api/chirps
```

```
GET /api/chirps
```

```
GET /api/chirps/{id}
```

```
POST /api/users
```
